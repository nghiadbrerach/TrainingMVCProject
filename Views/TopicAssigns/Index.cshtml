@model IEnumerable<TrainingMVCProject.Models.TopicAssign>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Course.CourseName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Topic.TopicName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Course.CourseName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Topic.TopicName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.TopicAssignID }) |
            @Html.ActionLink("Details", "Details", new { id=item.TopicAssignID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.TopicAssignID })
        </td>
    </tr>
}

</table>
